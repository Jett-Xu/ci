{
  "name": "ci-check",
  "version": "1.0.0",
  "description": "Standalone CI validation toolkit — strict, zero-warning checks for frontend and backend projects.",
  "private": true,
  "bin": {
    "ci-check": "bin/ci-check.js"
  },
  "engines": {
    "node": ">=16"
  }
}
